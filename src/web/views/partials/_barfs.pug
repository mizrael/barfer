div found #{barfs.totalCount} barfs
div(class="card-columns")
  each item in barfs.items
      div(class="card")
        blockquote(class="blockquote mb-0 card-body")
          p #{item.text}
          footer(class="blockquote-footer")
            small(class="text-muted") #{item.userName}