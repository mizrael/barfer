div found #{barfs.length} barfs
div(class="card-deck")
  each item in barfs
      div(class="card")
        blockquote(class="blockquote mb-0 card-body")
          p #{item.text}
          footer(class="blockquote-footer")
            small(class="text-muted") #{item.userName}