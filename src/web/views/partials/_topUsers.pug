h4 Top Users
ul(class="list-group")
  each item in topUsers
      li(class="list-group-item")
        p
          img(src=item.picture, title=item.name, alt=item.name)
          span  #{item.name}
        p @#{item.nickname}